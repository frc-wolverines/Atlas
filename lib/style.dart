import 'package:flutter/material.dart';

const maize = Color.fromARGB(255, 255, 241, 100);
const navyBlue = Color.fromARGB(255, 0, 0, 25);

const newMappingTextStyle = TextStyle(color: navyBlue, fontStyle: FontStyle.italic);